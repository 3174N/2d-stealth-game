using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[CreateAssetMenu(fileName = "New Coin", menuName = "Distractions/Coin")]
public class Coin : Distraction
{
    // Start is called before the first frame update
    void Start()
    {
        soundRadius = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
